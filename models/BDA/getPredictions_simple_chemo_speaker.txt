var predictives = map(function(d) {
    // console.log(d.utterance)
    // console.log(d.item)

var m = extend(model(estimates))
var beliefs_response = {"speaker_belief": d.speaker_response, "ah_belief": d.ah_response}
console.log(beliefs_response)
var speaker = viz_speaker(beliefs_response, d.qud, m, estimates)
console.log(Object.values(speaker.params.dist))

// console.log(prob)
// return {utterance: d.utterance, item: d.item, dist: belief_tuple_dist}
return {belief_tuple: beliefs_response, qud: d.qud, predictives: Object.values(speaker.params.dist)}
  
}, df)

predictives